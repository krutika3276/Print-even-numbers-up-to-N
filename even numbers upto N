#include <stdio.h>

int main() {
    int N, i = 2;

    printf("Enter a number: ");
    scanf("%d", &N);

    printf("Even numbers %d are\n", N);

    while(i <= N) {
        printf("%d ", i);
        i = i + 2;  
    }
